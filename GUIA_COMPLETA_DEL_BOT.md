# 📖 دليل شامل — بوت EXE - Store

هذا الملف يشرح بنية المشروع بالكامل وطريقة تشغيله وإدارته يوم بيوم. اقرأه كمرجع كل ما احتجت تتذكر حاجة أو تشرح المشروع لحد.

---

## 1) الفكرة العامة

البوت ده متجر تليجرام لبيع منتجات رقمية (اشتراكات، حسابات، أكواد...). شغال بطريقتين مع بعض:

- **منتجات KOKORO**: بتتزامن تلقائيًا كل شوية دقايق من API مزوّد الجملة (KOKORO)، وأنت بس بتحدد **هامش ربحك** لكل منتج والبوت بيبيعه ويسلمه أوتوماتيك.
- **منتجاتك اليدوية**: منتجات إنت بتضيفها بنفسك (اسم + سعر + أكواد/حسابات)، والبوت بيسلمها من مخزونك أوتوماتيك برضه.

العميل بيشوف البوت بالعربي أو الإنجليزي (حسب اختياره)، ويدفع عن طريق **Binance Pay** أو **BEP20 (USDT على شبكة BSC)** أو من **رصيده المخزّن** في البوت.

فيه كمان **لوحة تحكم ويب** منفصلة تدير كل حاجة (منتجات، مخزون، عملاء، طلبات، بث رسائل...) من غير ما تدخل تليجرام أو Supabase يدويًا.

---

## 2) بنية الملفات

```
ENVIAR-A-REVENDEDOR/
│
├─ start.js              ← نقطة تشغيل البوت (بيثبّت المكتبات أول مرة ويشغّل botlite.js)
├─ botlite.js             ← ملف البوت الرئيسي (كل منطق التليجرام: قوائم، شراء، دفع، تسليم...)
├─ package.json           ← تعريف المشروع والمكتبات المطلوبة
├─ setup.sql              ← كل أوامر إنشاء/تحديث جداول Supabase (تشغّله من SQL Editor)
├─ .env                   ← كل الإعدادات السرية (توكنات، مفاتيح API، بيانات لوحة الأدمن...)
├─ .env.example           ← نفس الملف لكن فاضي، للمرجع بس
│
├─ services/               ← كود مساعد يستخدمه البوت
│   ├─ kokoroApi.js         → التواصل مع API مزوّد الجملة KOKORO (منتجات، رصيد، شراء)
│   ├─ sync.js               → مزامنة منتجات KOKORO مع جدول products كل X دقيقة
│   ├─ payments.js           → التحقق من مدفوعات Binance Pay و BEP20
│   └─ emojis.js             → اختيار إيموجي مناسب لكل منتج حسب اسمه
│
├─ api/                    ← لوحة تحكم الأدمن (Backend) — دوال منفصلة تُنشر على Vercel
│   ├─ _lib/                 → كود مشترك (تسجيل الدخول، الاتصال بـ Supabase، إرسال تليجرام)
│   ├─ dashboard.js          → إحصائيات لوحة التحكم
│   ├─ products.js           → إدارة منتجات KOKORO (هامش، تفعيل، اسم، إيموجي)
│   ├─ products-manual.js    → إدارة منتجاتك اليدوية (إضافة/تعديل/حذف)
│   ├─ stock-manual.js       → إدارة مخزون (أكواد/حسابات) المنتجات اليدوية
│   ├─ users.js              → إدارة العملاء وأرصدتهم
│   ├─ orders.js             → إدارة الطلبات (تسليم يدوي/إلغاء)
│   ├─ transactions.js       → سجل حركات الأرصدة
│   ├─ email-activation.js   → قائمة المنتجات اللي بتتفعّل يدويًا (بريد/يوزر)
│   └─ broadcast.js          → إرسال رسالة لكل العملاء أو عميل محدد
│
├─ public/                 ← لوحة تحكم الأدمن (الواجهة اللي بتشوفها في المتصفح)
│   ├─ index.html
│   ├─ app.js
│   └─ style.css
│
├─ GUIA_ACTIVACION_Y_ENTREGA_MANUAL.md   ← شرح تفصيلي لنظام "التفعيل بالبريد/اليوزر"
└─ README.md                              ← دليل تركيب مختصر (بالإسباني، أساسي)
```

> ملفات `Como funciona INGLES.txt` و `como funciona ESPANOL.txt` قديمة من نسخة سابقة من البوت، مش لازم تتابعها.

---

## 3) قاعدة البيانات (Supabase)

كل البيانات متخزنة في مشروع Supabase بتاعك. الجداول دي بينشئها/يحدّثها ملف [setup.sql](setup.sql):

| الجدول | الوظيفة |
|---|---|
| `users` | عملاء البوت: ID تليجرام، اليوزر، اللغة، الرصيد |
| `products` | منتجات KOKORO المتزامنة تلقائيًا (اسم، سعر مايسترو، مخزون، هامشك، تفعيل، اسم مخصص) |
| `products_manual` | منتجاتك اليدوية (اسم، سعر نهائي، وصف عربي/إنجليزي) |
| `stock_manual` | الأكواد/الحسابات الفعلية لكل منتج يدوي — كل سطر = وحدة تُسلَّم مرة واحدة |
| `orders` | كل الطلبات (منتج، كمية، سعر، حالة، محتوى التسليم) |
| `transactions` | سجل حركات رصيد العملاء (شحن، شراء، تعديل يدوي) |
| `bep20_pending` | عمليات شحن/شراء BEP20 بانتظار وصول الدفعة |
| `binance_payments` | أرقام معاملات Binance/BEP20 المستخدمة (لمنع تكرار الاعتماد) |
| `email_activation_products` | أسماء المنتجات اللي بتتفعّل يدويًا بدل التسليم الأوتوماتيكي |

⚠️ **مهم:** كل مرة أضيف فيها عمود جديد للجداول (زي `custom_name` أو `markup_type` أو `description_ar`)، لازم تدخل Supabase → **SQL Editor** → تلصق **كل** محتوى `setup.sql` وتدوس **Run**. آمن تكرره أي وقت (مش هيمسح بيانات موجودة).

---

## 4) متغيرات الإعداد (`.env`)

| المتغير | الوظيفة |
|---|---|
| `BOT_TOKEN` | توكن البوت من BotFather |
| `ADMIN_LOG_GROUP` | ID جروب تليجرام تستقبل فيه سجلات الطلبات والتسليم اليدوي |
| `ADMIN_ID` | رقمك على تليجرام (مرجعي، مش مستخدم في الكود حاليًا) |
| `SHOP_NAME` | اسم متجرك الظاهر للعميل (`EXE - Store`) |
| `CHANNEL_URL` / `SUPPORT_URL` | روابط قناتك ودعمك (اختياري) |
| `KOKORO_API_URL` / `KOKORO_API_KEY` | الاتصال بمزوّد الجملة KOKORO |
| `SUPABASE_URL` / `SUPABASE_KEY` | الاتصال بقاعدة البيانات (المفتاح لازم يكون `service_role`) |
| `BINANCE_API_KEY` / `BINANCE_SECRET_KEY` | التحقق من مدفوعات Binance Pay |
| `BINANCE_PAY_ID` / `BINANCE_PAY_NAME` | بيانات استقبال الدفع عبر Binance Pay |
| `BEP20_WALLET` | عنوان محفظتك لاستقبال USDT (شبكة BSC) |
| `SYNC_INTERVAL_MINUTES` | كل كام دقيقة يتزامن مع KOKORO (افتراضي 5) |
| `ADMIN_PANEL_USER` / `ADMIN_PANEL_PASSWORD` | بيانات دخول لوحة التحكم الويب |
| `ADMIN_PANEL_SECRET` | مفتاح سري لتوقيع جلسة الدخول (لا تشاركه) |
| `ICON_*` | أكواد إيموجي متحركة خاصة بتليجرام — **لا تلمسها** |

---

## 5) رحلة العميل داخل البوت

1. **`/start`** → لو `CHANNEL_URL` مفعّل، بيُطلب من العميل الانضمام للقناة أولًا.
2. **اختيار اللغة** (عربي 🇸🇦 / إنجليزي 🇺🇸) — أول مرة بس، بعدين بيتذكرها.
3. **القائمة الرئيسية**: المتجر، رصيدي، شحن المحفظة، مشترياتي، اللغة.
4. **المتجر**: منتجات مقسّمة لفئات تلقائيًا حسب الاسم (Cursor، Disney، Adobe، متابعين...).
5. **صفحة المنتج**: السعر، الوصف، خصومات الكمية (لو موجودة)، زرار "اشترِ الآن".
6. **الدفع**: من الرصيد المخزَّن، أو Binance Pay، أو BEP20.
7. **التسليم**:
   - منتجات KOKORO والمنتجات اليدوية العادية → تسليم **أوتوماتيكي فوري**.
   - منتجات في قائمة "التفعيل بالبريد" → البوت يطلب من العميل بريده/يوزره، ويوصلك إشعار في `ADMIN_LOG_GROUP`، وإنت تسلّمها يدويًا (من لوحة الأدمن أو أمر `/entregar`).

---

## 6) لوحة تحكم الأدمن (الويب)

الوصول: `http://localhost:3000` (محليًا) أو رابط Vercel بعد النشر — بيانات الدخول من `ADMIN_PANEL_USER` / `ADMIN_PANEL_PASSWORD`.

| التبويب | بيعمل إيه |
|---|---|
| 📊 لوحة التحكم | رصيد KOKORO، عدد العملاء، الطلبات، الإيرادات، طلبات محتاجة تسليم يدوي |
| 📣 بث رسالة | إرسال رسالة تليجرام لكل العملاء أو لعميل واحد بالـ ID |
| 📦 منتجات KOKORO | تعديل: الاسم المعروض، الهامش (% أو مبلغ ثابت $)، التفعيل (فوري)، الإيموجي |
| 🧩 منتجاتي اليدوية | إضافة منتج جديد **مع مخزونه** في خطوة واحدة، تعديل، حذف، إدارة المخزون لاحقًا |
| 👥 المستخدمين | بحث، وتعديل رصيد أي عميل يدويًا (بيوصله إشعار تليجرام تلقائي) |
| 🧾 الطلبات | فلترة بالحالة، تسليم يدوي (بيبعت للعميل ويسجل في ADMIN_LOG_GROUP)، إلغاء |
| 💳 المعاملات | سجل كل حركات الأرصدة |
| 📧 التفعيل بالبريد | إدارة قائمة المنتجات اللي بتتفعّل يدويًا (بديل أوامر `/correos_*`) |

**إشعارات تلقائية شغالة من اللوحة:**
- تعديل رصيد عميل → إشعار فوري للعميل بلغته.
- إضافة مخزون لمنتج يدوي → بث تلقائي لكل العملاء (تقدر توقفه بالـ checkbox).
- تسليم/إلغاء طلب → إشعار للعميل + سجل في `ADMIN_LOG_GROUP`.

---

## 7) أوامر تليجرام الإدارية (لسه شغالة، من داخل `ADMIN_LOG_GROUP`)

| الأمر | الوظيفة |
|---|---|
| `/entregar` | بدء تسليم يدوي لطلب (بديلها الأسرع: تبويب "الطلبات" في اللوحة) |
| `/cancelar` | إلغاء عملية تسليم يدوي جارية |
| `/correos_add <اسم المنتج بالضبط>` | إضافة منتج لقائمة "يتفعّل يدويًا" |
| `/correos_list` | عرض القائمة |
| `/correos_del <اسم المنتج بالضبط>` | حذف من القائمة (يرجع للتسليم الأوتوماتيكي) |

---

## 8) طريقة التشغيل

### تشغيل البوت
```bash
node start.js
```
أول مرة بيثبّت المكتبات لوحده (بياخد دقيقة، دقيقتين)، وبعدين بيشتغل على طول. لو قفلت الـ terminal، البوت بيقف — عشان يفضل شغال 24/7 استخدم **PM2**:
```bash
npm install -g pm2
pm2 start start.js --name exe-store
pm2 save
pm2 startup
```
أوامر يومية: `pm2 restart exe-store` / `pm2 stop exe-store` / `pm2 logs exe-store`.

### تشغيل لوحة الأدمن محليًا (للتجربة)
```bash
node _devserver.local.js
```
وافتح `http://localhost:3000`. (الملف ده للتجربة المحلية بس، مش بيتنشر مع المشروع).

### نشر لوحة الأدمن أونلاين (Vercel)
اللوحة (مجلدات `api/` و `public/`) مستقلة تمامًا عن البوت — تقدر تنشرها على Vercel بدون ما تأثر على تشغيل البوت:
```bash
npx vercel login
npx vercel --prod
```
وضيف في إعدادات البيئة على Vercel نفس القيم: `SUPABASE_URL`, `SUPABASE_KEY`, `BOT_TOKEN`, `ADMIN_LOG_GROUP`, `ADMIN_PANEL_USER`, `ADMIN_PANEL_PASSWORD`, `ADMIN_PANEL_SECRET`.

> ⚠️ **البوت نفسه (`botlite.js`) لازم يفضل شغال في مكان بيدعم Node مستمر** (جهازك بـ PM2، أو Railway/VPS) — Vercel لوحدها متعملش استضافة لبوت شغال بـ polling مستمر.

---

## 9) نصائح صيانة سريعة

- **منتج مش ظاهر في البوت؟** غالبًا سبب واحد من اتنين: (1) `enabled = false`، أو (2) المخزون/الستوك = صفر. راجعهم من اللوحة.
- **غيّرت اسم منتج KOKORO؟** التصنيف التلقائي للفئات (Cursor، Disney...) بيعتمد على كلمات إنجليزية في الاسم — لو مسحتها ممكن المنتج يروح لقسم عام.
- **بعد أي تحديث فيه أعمدة جداول جديدة**، شغّل `setup.sql` تاني في Supabase قبل ما تجرب الميزة الجديدة.
- **رصيد KOKORO لازم يكون فيه فلوس مسبقًا** عشان تقدر تبيع منتجات KOKORO (مش المنتجات اليدوية، دي بتاعتك إنت).
